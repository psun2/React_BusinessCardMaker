// const authProvider = new firebase.auth[`${providerName}AuthProvider`]();
// 구조 해부
class Test {
  ASDFGH = {
    asd: () => {
      console.log('작동 되나요?');
    },
  };
}

const provider = 'asd';

const test = new Test().ASDFGH[`${provider}`]();
